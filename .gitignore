# IDE
.vs/
*.user
*.suo
.DS_Store

# Local game refs for client builds (junctions / hardlinks to SPT install)
/BepInEx/
/EscapeFromTarkov_Data/
/EscapeFromTarkov.exe

# Build outputs
**/bin/
**/obj/
**/node_modules/

# Nested per-mod git metadata (sources are tracked in this monorepo)
# (git ignores .git dirs automatically; listed for clarity)

# Large local caches if any
**/*.log

# Scratch clones / one-off upstream diffs (not source of truth)
.tmp/

# Local VirusTotal upload staging
scripts/vt-uploads/

# Unpacked install pack (track only the zip)
mods_patch/SPT-*-ModPack/

# Downloaded release cache for manage-modpack scripts
mods_patch/.modpack-cache/
